const { router } = require('./alquiler');
const { router }  = require('./contratista');
const { router } = require('./estudiante');
const { router } = require('./factura');
const { router } = require('./habitacion');
const { router } = require('./tipo');
const { router } = require('./usuario');


module.exports = {
    router
}